import styled from "styled-components"

/**
 * Simple h2 tag, with some minor tweaks
 */
const StyledH2 = styled.h2`
  font-weight: 400;
  font-style: normal;
  color: gray;
  text-transform: uppercase;
  margin: 0;
`

export default StyledH2
